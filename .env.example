# Copy to .env (do not commit real secrets)

# PostgreSQL
# Prefer DATABASE_URL; app falls back to DATABASE_MT_URL
DATABASE_URL=postgresql://lachlan:change_me@localhost:5432/metatrader_db
# DATABASE_MT_URL=postgresql://lachlan:change_me@localhost:5432/metatrader_db

# MT5 Python bridge
# Point to your terminal64.exe inside the Wine prefix
MT5_PATH=/home/USER/.mt5/drive_c/Program Files/MetaTrader 5/terminal64.exe
MT5_LOGIN=68257735
MT5_PASSWORD=REPLACE_WITH_DEMO_PASSWORD
# MT5_SERVER=YourBroker-ServerName  # optional if terminal already logged in

# Web server
PORT=8888

